{% extends "apps/wp/templates/download/form.twig" %}

{% block main %}

  <div class="row">
    <div class="col-xs-3"></div>
    <div class="col-xs-6">
      {% for field in form.visible %}
        {{ field.field|raw }}
      {% endfor %}

      {% for field in form.hidden %}
        {{ field.field|raw }}
      {% endfor %}
    </div>
    <div class="col-xs-3"></div>
  </div>

  <div id="facebook_page_like">
    <i class="fa fa-spinner fa-spin"></i> Loading
  </div>
  
  <script>
    $(function() {
      facebook_page_id = "{{ targeturl.wapo.external }}";
      facebook_ready(facebook_page_likes);
    });
  </script>
{% endblock main %}